base="<database name>"
host="<database endpoint>"
user="<database username>"
pass="<database user password>"
path="<full path to the ClimateService folder>"
poly="<path to the clip mask GeoJSON file (see gdalwarp)>"
pnam="<clip mask layer name (see gdalwarp)>"
pref="<prefix for all variable names: will become prefix_name>"
